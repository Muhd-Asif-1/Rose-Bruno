meta {
  name: Request Completion OTP
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/app/provider/bookings/{{bookingId}}/completion-otp
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{providerAccessToken}}
}
