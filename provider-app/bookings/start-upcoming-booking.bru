meta {
  name: Start Upcoming Booking
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/app/provider/bookings/{{bookingId}}/status
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{providerAccessToken}}
  Content-Type: application/json
}

body:json {
  {
    "status": "in_progress"
  }
}
