meta {
  name: Download Message Image
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/app/provider/bookings/{{bookingId}}/messages/{{chatImageMessageId}}/image
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{providerAccessToken}}
}
