meta {
  name: Toggle Favourite
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/app/customer/favourites
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{customerAccessToken}}
  Content-Type: application/json
}

body:json {
  {
    "providerId": "{{providerId}}"
  }
}
